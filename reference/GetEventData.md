# Get splice event annotation table

Get splice event annotation table

## Usage

``` r
GetEventData(object, ...)

# S4 method for class 'MatisseObject'
GetEventData(object, ...)
```

## Arguments

- object:

  A `MatisseObject`.

- ...:

  Additional arguments (unused).

## Value

A `data.frame` of splice event metadata.
