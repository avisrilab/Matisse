# Get the embedded Seurat object

Get the embedded Seurat object

## Usage

``` r
GetSeurat(object, ...)

# S4 method for class 'MatisseObject'
GetSeurat(object, ...)
```

## Arguments

- object:

  A `MatisseObject`.

- ...:

  Additional arguments (unused).

## Value

A `Seurat` object.
