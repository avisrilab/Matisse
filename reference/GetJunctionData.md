# Get junction annotation table

Get junction annotation table

## Usage

``` r
GetJunctionData(object, ...)

# S4 method for class 'MatisseObject'
GetJunctionData(object, ...)
```

## Arguments

- object:

  A `MatisseObject`.

- ...:

  Additional arguments (unused).

## Value

A `data.frame` of junction metadata.
