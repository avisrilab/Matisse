% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ManualTranscriptCoveragePlot.R
\name{ManualTranscriptCoveragePlot}
\alias{ManualTranscriptCoveragePlot}
\title{Plot transcript coverage tracks using Signac coverage plotting utilities.}
\usage{
ManualTranscriptCoveragePlot(
  track.df,
  region = NULL,
  transcript.col = c("transcript_name", "transcript_id"),
  window = NA,
  ymax = NULL,
  split.assays = TRUE,
  assay.scale = "separate",
  annotation = FALSE,
  object = NULL,
  assay = NULL,
  heights = NULL
)
}
\arguments{
\item{track.df}{Data frame of transcript coverage tracks. Must contain genomic
coordinates, the required \code{plot_value} column, and the requested transcript
identifier column.}

\item{region}{Optional \code{GenomicRanges::GRanges} region to plot. If \code{NULL},
the region is inferred from \code{track.df}.}

\item{transcript.col}{Character(1). Which transcript identifier column to use;
one of \code{"transcript_name"} or \code{"transcript_id"}.}

\item{window}{Numeric(1) or \code{NA}. Passed to \code{Signac:::CoverageTrack()} for
smoothing.}

\item{ymax}{Optional numeric. Passed to \code{Signac:::CoverageTrack()}.}

\item{split.assays}{Logical(1). Passed to \code{Signac:::CoverageTrack()}.}

\item{assay.scale}{Character(1). Passed to \code{Signac:::CoverageTrack()}.}

\item{annotation}{Logical(1). If \code{TRUE}, add a gene annotation track.}

\item{object}{Optional Seurat object. Required if \code{annotation = TRUE}.}

\item{assay}{Optional Character(1). Assay name to use for annotation plotting.
Required if \code{annotation = TRUE}.}

\item{heights}{Optional numeric vector. Passed to \code{Signac:::CombineTracks()}.}
}
\value{
A ggplot/patchwork-style combined coverage plot.
}
\description{
This function plots transcript coverage tracks using the mandatory normalized
plotting column \code{plot_value} generated upstream by
\code{PrepareTranscriptCoverageTracks()}. Group scaling and global scaling are
therefore assumed to already be baked into \code{track.df}. Smoothing is applied
here through the \code{window} argument passed to \code{Signac:::CoverageTrack()}.
}
