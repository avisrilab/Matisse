% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ManualTranscriptCoveragePlotWithModel.R
\name{ManualTranscriptCoveragePlotWithModel}
\alias{ManualTranscriptCoveragePlotWithModel}
\title{Plot transcript coverage together with a transcript model.}
\usage{
ManualTranscriptCoveragePlotWithModel(
  track.df,
  model,
  region = NULL,
  transcript.col = c("transcript_name", "transcript_id"),
  window = NA,
  ymax = NULL,
  split.assays = TRUE,
  assay.scale = "separate",
  model.height = 0.4
)
}
\arguments{
\item{track.df}{Data frame of transcript coverage tracks. Must contain genomic
coordinates, \code{plot_value}, and the requested transcript identifier column.}

\item{model}{List produced by \code{BuildGeneModel()}. Passed to
\code{PlotTranscriptModel()}.}

\item{region}{Optional \code{GenomicRanges::GRanges} region to plot. If \code{NULL},
the region is inferred from \code{track.df}.}

\item{transcript.col}{Character(1). Which transcript identifier column to use;
one of \code{"transcript_name"} or \code{"transcript_id"}.}

\item{window}{Numeric(1) or \code{NA}. Passed to \code{ManualTranscriptCoveragePlot()}.}

\item{ymax}{Optional numeric. Passed to \code{ManualTranscriptCoveragePlot()}.}

\item{split.assays}{Logical(1). Passed to \code{ManualTranscriptCoveragePlot()}.}

\item{assay.scale}{Character(1). Passed to \code{ManualTranscriptCoveragePlot()}.}

\item{model.height}{Numeric(1). Relative height of the transcript model panel.}
}
\value{
A combined patchwork plot containing transcript coverage and transcript
model tracks.
}
\description{
This function combines the baked-in normalized transcript coverage plot with a
transcript model panel. The coverage plot always uses the mandatory
\code{plot_value} column produced upstream by \code{PrepareTranscriptCoverageTracks()}.
}
