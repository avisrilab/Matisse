% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TracksToCutMatList.R
\name{TracksToCutmatList}
\alias{TracksToCutmatList}
\title{Convert transcript coverage tracks into a cut-matrix list for plotting.}
\usage{
TracksToCutmatList(
  track.df,
  region = NULL,
  transcript.col = c("transcript_name", "transcript_id")
)
}
\arguments{
\item{track.df}{Data frame of transcript coverage tracks. Must contain genomic
coordinates, grouping information or fields sufficient to create it, the
required \code{plot_value} column, and the requested transcript identifier column.}

\item{region}{Optional \code{GenomicRanges::GRanges} object defining the plotting
region. If \code{NULL}, the region is inferred from \code{track.df}.}

\item{transcript.col}{Character(1). Which transcript identifier column to use;
one of \code{"transcript_name"} or \code{"transcript_id"}.}
}
\value{
A list containing a transcript-named cut-matrix list, grouping factor,
and genomic region.
}
\description{
This function converts plot-ready transcript coverage tracks into a
transcript-named list of group x genomic-position matrices. It uses the
mandatory normalized plotting column \code{plot_value} generated upstream by
\code{PrepareTranscriptCoverageTracks()}.
}
