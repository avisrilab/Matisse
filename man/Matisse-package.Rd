% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Matisse-package.R
\docType{package}
\name{Matisse-package}
\alias{Matisse}
\alias{Matisse-package}
\title{Matisse: Multi-modal Analysis of Transcript Isoforms in Single-Cell
Sequencing Experiments}
\description{
Matisse provides an integrated framework for isoform-resolved single-cell
RNA-seq analysis, built on top of \pkg{Seurat} and \pkg{Signac}.

Key capabilities:
\itemize{
\item \strong{MatisseObject} -- an S4 class that wraps a \code{Seurat}
object and co-stores raw isoform counts, PSI matrices, and splice event
annotations, keeping gene expression and isoform layers synchronised.
\item \strong{Single-step construction with PSI} --
\code{\link{CreateMatisseObject}} computes per-cell Percent Spliced In
values as part of construction (pass \code{defer_psi = TRUE} to skip).
Works in both junction mode (STARsolo) and transcript mode (Bagpiper,
FLAMES, LIQA).
\item \strong{Isoform QC} -- \code{nCount_isoform} and
\code{nFeature_isoform} are written at construction;
\code{nPercent_isoform} is written by the PSI step.
\code{\link{FilterCells}} and \code{\link{FilterEvents}} enforce
quality thresholds.
\item \strong{Visualization} -- UMAP overlays, violin plots, PSI
heatmaps, and sashimi junction-arc plots via a consistent
ggplot2-based API.
}
}
\section{Storage convention}{

Matisse stores everything inside the embedded Seurat object. To access
raw data with native Seurat verbs:
\describe{
\item{Junction or transcript counts}{
\code{SeuratObject::GetAssayData(GetSeurat(obj)[["isoform"]], "counts")}
}
\item{PSI values (events x cells)}{
\code{SeuratObject::GetAssayData(GetSeurat(obj)[["psi"]], "data")}
}
\item{Inclusion / exclusion read counts}{
\code{SeuratObject::GetAssayData(GetSeurat(obj)[["psi"]], "counts")} and
\code{... ([["psi"]], "exclusion")}
}
\item{Per-event annotation}{
\code{GetSeurat(obj)[["psi"]][[]]} (data.frame keyed by event_id rownames)
}
\item{Per-junction coordinates (junction mode)}{
\code{GetSeurat(obj)[["isoform"]][[]]} -- chr/start/end/strand auto-parsed
from junction IDs at construction.
}
\item{Per-cell metadata}{
\code{MatisseMeta(obj)} or \code{GetSeurat(obj)@meta.data}
}
}
}

\section{Package website}{

Full documentation and vignettes are available at
\url{https://avisrilab.github.io/Matisse}.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://avisrilab.github.io/Matisse}
  \item \url{https://github.com/avisrilab/Matisse}
  \item Report bugs at \url{https://github.com/avisrilab/Matisse/issues}
}

}
\author{
\strong{Maintainer}: k3yavi \email{avisrilab@gmail.com}

}
\keyword{internal}
