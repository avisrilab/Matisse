% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/normalization.R
\name{SCTransformTranscripts}
\alias{SCTransformTranscripts}
\alias{SCTransformTranscripts,MatisseObject-method}
\title{SCTransform normalization for the transcript assay}
\usage{
SCTransformTranscripts(object, ...)

\S4method{SCTransformTranscripts}{MatisseObject}(
  object,
  n_pca_dims = 50L,
  vars_to_regress = NULL,
  assay = "transcript",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{\linkS4class{MatisseObject}} with a
\code{"transcript"} assay. Use
\code{\link{CreateMatisseObjectFromTranscripts}} or
\code{CreateMatisseObject(transcript_counts = ...)} to create one.}

\item{...}{Additional arguments forwarded to \code{\link[Seurat]{SCTransform}}.}

\item{n_pca_dims}{Integer. Number of principal components to compute.
Default: \code{50}.}

\item{vars_to_regress}{Character vector of variables to regress out during
SCTransform (e.g. \code{"percent.mt"}). Default: \code{NULL}.}

\item{assay}{Character. Name of the assay to run SCTransform on. Default:
\code{"transcript"}.}

\item{verbose}{Logical. Default: \code{TRUE}.}
}
\value{
The updated \code{MatisseObject}.

The updated \code{MatisseObject} with:
\itemize{
\item An \code{"SCT"} assay (created by SCTransform) in the Seurat object.
\item A \code{"pca"} reduction computed on the SCT assay.
}
}
\description{
Runs \code{SCTransform} on the \code{"transcript"} assay of the embedded
Seurat object and follows it with \code{RunPCA} using a larger number of
principal components, giving better isoform-level cluster resolution.

Runs \code{\link[Seurat]{SCTransform}} on the \code{"transcript"}
\code{Assay5} of the embedded Seurat object, followed by
\code{\link[Seurat]{RunPCA}} with \code{n_pca_dims} components.
}
\details{
SCTransform is better suited than standard log-normalization for
transcript-level count data: it corrects for sequencing depth while
preserving biological variance. Using a higher number of PCA dimensions
(default 50) has been found to resolve more isoform-level clusters than the
standard 20.

After this step you can call \code{\link[Seurat]{FindNeighbors}},
\code{\link[Seurat]{FindClusters}}, and \code{\link[Seurat]{RunUMAP}}
directly on the \code{MatisseObject} via the built-in method forwarding
(see \code{\link{MatisseObject-class}}).
}
\seealso{
\code{\link{CreateMatisseObjectFromTranscripts}},
\code{\link[Seurat]{SCTransform}}, \code{\link[Seurat]{RunPCA}}
}
