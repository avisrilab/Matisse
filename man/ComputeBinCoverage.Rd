% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComputeBinCoverage.R
\name{ComputeBinCoverage}
\alias{ComputeBinCoverage}
\title{Compute per-cell bin support from a BAM file.}
\usage{
ComputeBinCoverage(
  bam,
  bins,
  region = NULL,
  barcode.field = 2,
  qname.sep = "_"
)
}
\arguments{
\item{bam}{Character(1). Path to a BAM file.}

\item{bins}{A \code{GenomicRanges::GRanges} object of genomic bins. Must contain a
\code{bin_id} metadata column.}

\item{region}{Optional \code{GenomicRanges::GRanges} object specifying the genomic
region to read from the BAM. If \code{NULL}, the range of \code{bins} is used.}

\item{barcode.field}{Integer(1). Position of the cell barcode after splitting
each read name by \code{qname.sep}.}

\item{qname.sep}{Character(1). Delimiter used to split read names.}
}
\value{
A list containing:
\describe{
\item{signal_matrix}{Sparse cell-by-bin matrix of summed fractional bin support.}
\item{read_matrix}{Sparse cell-by-bin matrix of distinct read counts contributing to each bin.}
\item{cells}{Character vector of cell barcodes.}
\item{bins}{Input \code{GRanges} of bins.}
}
}
\description{
This function computes the unnormalized per-cell bin-level inputs used by the
downstream transcript-resolution and plotting-normalization workflow.
}
\details{
For each cell x bin combination, it returns:
\enumerate{
\item A signal value defined as the sum over reads of (overlap width / bin width)
\item A read-support value defined as the number of distinct reads contributing
support to the bin
}

These outputs are intentionally left unnormalized here. Group scaling,
global scaling, and smoothing are applied later in the plotting workflow.
}
