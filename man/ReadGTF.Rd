% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReadGTF.R
\name{ReadGTF}
\alias{ReadGTF}
\title{Read a GTF and extract transcript-level annotations aligned to a transcript matrix}
\usage{
ReadGTF(
  tmat,
  gtf.file,
  transcript.field = "transcript_id",
  gene.field = "gene_name",
  transcript.type.field = "transcript_type",
  include.gene_id = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{tmat}{A sparse matrix with transcripts/isoforms as rows and cells as columns.}

\item{gtf.file}{Character(1). Path to a GTF file (.gtf or .gtf.gz).}

\item{transcript.field}{Character(1). Attribute key in the GTF to match transcripts in \code{tmat} (default = "transcript_id").}

\item{gene.field}{Character(1). Attribute key for the gene label to return (default = "gene_name").}

\item{transcript.type.field}{Character(1). Attribute key for transcript type/biotype to return (default = "transcript_type").}

\item{include.gene_id}{Logical(1). If TRUE, also extracts \code{gene_id} into a \code{gene.id} column (default = FALSE).}

\item{verbose}{Logical(1). If TRUE, prints basic progress messages (default = FALSE).}
}
\value{
A data.frame with one row per transcript in \code{tmat}, containing:
\code{transcript_id}, \code{gene}, \code{transcript.type}, \code{seqname}, \code{start}, \code{end}, \code{strand}, and optional \code{gene.id}.
}
\description{
Read a GTF and extract transcript-level annotations aligned to a transcript matrix
}
