% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/visualization.R
\name{PlotSashimi}
\alias{PlotSashimi}
\alias{PlotSashimi,MatisseObject-method}
\title{Sashimi-style coverage plot for a splice event}
\usage{
PlotSashimi(
  object,
  event_id,
  cells = NULL,
  group_by = NULL,
  arc_scale = c("sqrt", "linear", "log"),
  colours = c(inclusion = "#4393c3", exclusion = "#d6604d"),
  title = NULL,
  ...
)

\S4method{PlotSashimi}{MatisseObject}(
  object,
  event_id,
  cells = NULL,
  group_by = NULL,
  arc_scale = c("sqrt", "linear", "log"),
  colours = c(inclusion = "#4393c3", exclusion = "#d6604d"),
  title = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{MatisseObject} with a PSI assay computed.}

\item{event_id}{Character. A single event ID. Run
\code{rownames(GetSeurat(obj)[["psi"]])} to list available IDs;
typical SE format is \code{"SE:chr1:1201-2999:3201-4999:+"}.}

\item{cells}{Character vector of cell barcodes to aggregate over.
Default: all cells.}

\item{group_by}{Character. Column in Seurat meta.data to facet by.
Default: \code{NULL} (all cells pooled).}

\item{arc_scale}{Character. How to scale arc height to read count:
\code{"sqrt"} (default), \code{"linear"}, or \code{"log"}.}

\item{colours}{Named character vector with elements \code{"inclusion"} and
\code{"exclusion"} giving arc colours.}

\item{title}{Character. Plot title. Defaults to \code{event_id}.}

\item{...}{Additional arguments (see \code{\link{PlotSashimi}}).}
}
\value{
A \code{ggplot} object.

A \code{ggplot} object.
}
\description{
Draws junction arcs scaled by read count over a schematic gene structure.
Arcs are coloured by role: inclusion (blue) vs exclusion (red). Works in
both junction mode and transcript mode. Optionally faceted by a cell
metadata column.

Draws junction arcs scaled by aggregate read count over a schematic gene
structure. Arcs are coloured by role: inclusion (blue) vs exclusion (red).
}
\details{
In \strong{junction mode} each arc corresponds to an individual junction
with its own read count. In \strong{transcript mode} the SE event_id is
parsed to derive junction coordinates; inclusion and exclusion counts come
from the \code{"counts"} and \code{"exclusion"} layers of the PSI assay.

Supported event types in transcript mode: every SUPPA2 type Matisse
ingests -- \strong{SE} (skipped exon), \strong{RI} (retained intron),
\strong{A3}/\strong{A5} (alternative 3'/5' splice site), \strong{AF}/
\strong{AL} (alternative first/last exon), and \strong{MX} (mutually
exclusive exons). Junction coordinates for the SUPPA2 grammar are parsed
with the same logic as the short-read junction adapter. Junction mode
supports all event types since coordinates are auto-parsed from
junction IDs.

\strong{Note on transcript-mode multi-arc events:} transcript-level
counting aggregates reads to events, not to individual junctions. When
an event side has more than one defining junction (the two SE / MX
inclusion arcs, the two MX exclusion arcs), the total event count for
that side is split evenly across its arcs. The arcs may have differed in
reality; use junction-mode input if you need per-junction read counts.
}
