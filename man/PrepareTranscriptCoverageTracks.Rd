% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PrepareTranscriptCoverageTracks.R
\name{PrepareTranscriptCoverageTracks}
\alias{PrepareTranscriptCoverageTracks}
\title{Prepare transcript coverage tracks for plotting.}
\usage{
PrepareTranscriptCoverageTracks(signal.df, read.df, model)
}
\arguments{
\item{signal.df}{Data frame of transcript-resolved signal. Must contain at least
\code{value}, \code{transcript_id}, \code{transcript_name}, and \code{bin_id}. If \code{group} is not
present, it will be added as \code{"All cells"}.}

\item{read.df}{Data frame of transcript-resolved read support. Must contain at
least \code{value}, \code{transcript_id}, \code{transcript_name}, and \code{bin_id}. If \code{group}
is not present, it will be added as \code{"All cells"}.}

\item{model}{List produced by \code{BuildGeneModel()}. Must contain \code{bins} with
required bin-level metadata.}
}
\value{
A data frame containing aggregated signal, read support, mandatory
normalization factors, and genomic bin coordinates for plotting. The column
\code{plot_value} is the only value intended for downstream plotting.
}
\description{
This function aggregates transcript-resolved signal and read support to the
group x transcript x bin level, computes mandatory Signac-style normalization,
and returns a plot-ready track table with a single \code{plot_value} column used
downstream for visualization.
}
